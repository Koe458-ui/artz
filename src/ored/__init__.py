"""Ored.ai -- an AI built from scratch.

Package layout
--------------
ored.config      configuration objects loaded from YAML
ored.data        dataset generation, preprocessing, batching
ored.models      neural-network architectures
ored.training    the training loop (forward, loss, backward, update)
ored.evaluation  measuring how good a trained model is
ored.inference   using a trained model on new input
ored.utils       seeding, logging, checkpoint I/O, weight statistics
"""

__version__ = "0.1.0"
